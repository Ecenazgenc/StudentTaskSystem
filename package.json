{
  "name": "student-task-system-root",
  "version": "1.0.0",
  "description": "Öğrenci Görev Takip Sistemi - Single Command Runner",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"cyan,magenta\" \"cd backend && mvnw.cmd spring-boot:run\" \"cd frontend && npm run dev\"",
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
